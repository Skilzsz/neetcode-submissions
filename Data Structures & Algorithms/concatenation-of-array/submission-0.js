class Solution {
    /**
     * @param {number[]} nums
     * @return {number[]}
     */
    getConcatenation(nums) {
        let newa = nums;
        let additional = nums;
        return newa.concat(additional)
    }
}


//I: Arrray of integers
//O: array of nums that are duplicated. 
    //new array will contain the concatenation of the two num arrays. 